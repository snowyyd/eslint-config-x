// import pkgJson from '../../package.json' with { type: 'json' };

const name = '@snowyyd/eslint-config-x';

export const constants = {
	baseName: `${name}/`,
	airbnbBaseName: `${name}/rules/airbnb-base/`,
};
